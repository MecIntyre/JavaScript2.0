"use strict"

let request = null
request = "GET /"

/*
JavaScript erkennt automatisch, dass die Variable ein String ist und einen Wert enthält.
Daraufhin wird die Variable in der If-Abfrage direkt jenach Wert, in ein True/False umgewandelt. */

if (request) {
    console.log("Hallo Welt!")
}